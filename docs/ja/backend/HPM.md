# HPM

- バージョン: V2R2
- ステータス: OK
- 日付: 2025/02/27
- コミット: [xxx](https://github.com/OpenXiangShan/XiangShan/tree/xxx)

## 基本情報

### 用語説明

表: 用語説明

| 略語 | 正式名称 | 説明 |
|---|---|---|
| HPM | Hardware performance monitor | ハードウェアパフォーマンスカウンタユニット |

### サブモジュールリスト

表: サブモジュールリスト

| サブモジュール | 説明 |
|---|---|
| HPerfCounter | 単一カウンタモジュール |
| HPerfMonitor | カウンタ組織モジュール |
| PFEvent | Hpmeventレジスタのコピー |

### 設計仕様

- RISC-V特権マニュアルに基づいた基本的なハードウェアパフォーマンスモニタリング機能を実装し、さらにsstcおよびsscofpmf拡張をサポート
- ハードウェアスレッドが実行したクロックサイクル数 (cycle)
- ハードウェアスレッドがコミットした命令数 (minstret)
- ハードウェアタイマー (time)
- カウンタオーバーフローフラグ (time)
- 29個のハードウェアパフォーマンスカウンタ (hpmcounter3 - hpmcouonter3)
- 29個のハードウェアパフォーマンスイベントセレクタ (mhpmcounter3 - mhpmcounter31)
- 最大2^10種類のパフォーマンスイベントの定義をサポート

### 機能

HPMの基本機能は以下の通りです。

* mcountinhibitレジスタを介してすべてのパフォーマンスイベントモニタリングを無効化します。
* 各モニタリングユニットのパフォーマンスイベントカウンタを初期化します。これには、mcycle, minstret, mhpmcounter3 - mhpmcounter31が含まれます。
* 各モニタリングユニットのパフォーマンスイベントセレクタを設定します。これには、mhpmcounter3 - mhpmcounter31が含まれます。香山昆明湖アーキテクチャでは、各イベントセレクタに最大4つのイベントの組み合わせを設定できます。イベントインデックス値、イベント組み合わせ方法、サンプリング特権レベルをイベントセレクタに書き込むと、指定されたサンプリング特権レベルで設定されたイベントを正常にカウントし、組み合わせ後の結果に基づいてイベントカウンタに累算します。
* xcounterenを設定してアクセス権限を付与します。
* mcountinhibitレジスタを介してすべてのパフォーマンスイベントモニタリングを有効にし、カウントを開始します。

#### HPMイベントオーバーフロー割り込み

昆明湖パフォーマンスモニタリングユニットが発行するオーバーフロー割り込みLCOFIPは、統一された割り込みベクタ番号12を持ちます。割り込みの有効化および処理プロセスは、通常のプライベート割り込みと同じです。

## 全体設計

各サブモジュール内でパフォーマンスイベントを定義し、サブモジュールはgeneratePerfEventを呼び出してパフォーマンスイベントをio_perfとして組み立て、Frontend, Backend, MemBlock, CoupledL2の4つの主要モジュールに出力します。

上記の4つのモジュールは、get_perfメソッドを呼び出してサブモジュールのパフォーマンスイベント出力を取得します。同時に、各主要モジュールはPFEventモジュールをインスタンス化し、CSR内のmhpmeventのレプリカとして、必要なパフォーマンスイベントセレクタデータとサブモジュールのパフォーマンスイベント出力セットをHPerfMonitorモジュールに接続し、パフォーマンスイベントカウンタに適用される増分結果を計算します。

最後に、CSRは4つのトップレベルモジュールからのパフォーマンスイベントカウンタの増分結果を収集し、それぞれCSRレジスタmhpmcounter3-31に入力して累計カウントを行います。

特に、CoupledL2のパフォーマンスイベントは直接CSRモジュールに入力され、mhpmeventレジスタから読み出されたイベント選択情報に基づいて、CSRにインスタンス化されたHPerfMonitorモジュールで処理された後、CSRレジスタmhpmcounter26-31に入力されて累計カウントされます。

具体的なHPMの全体設計ブロック図は[@fig:HPM]を参照してください。

![HPM全体設計](./figure/hpm.svg){#fig:HPM}

### HPerfMonitor カウンタ組織モジュール

入力されたイベント選択情報（events）を対応するHPerfCounterモジュールに入力し、すべてのパフォーマンスイベントカウント情報を各HperfCounterモジュールにコピーして入力します。

すべてのHperfCounterの出力を収集します。

### HperfCounter 単一カウンタモジュール

入力されたイベント選択情報に基づいて、必要なパフォーマンスイベントカウント情報を選択し、イベント選択情報内のカウントモードに従って、入力されたパフォーマンスイベントを組み合わせて出力します。

### PFEvent Hpmeventレジスタのコピー

CSRレジスタmhpmeventのコピー：CSRの書き込み情報を収集し、mhpmeventの変更を同期します。

## HPM関連の制御レジスタ

### マシンモードパフォーマンスイベントカウント禁止レジスタ (MCOUNTINHIBIT)

マシンモードパフォーマンスイベントカウント禁止レジスタ (mcountinhibit) は、32ビットのWARLレジスタで、主にハードウェアパフォーマンスモニタリングカウンタがカウントするかどうかを制御するために使用されます。パフォーマンス分析が不要なシナリオでは、カウンタをオフにしてプロセッサの消費電力を削減できます。

表: マシンモードパフォーマンスイベントカウント禁止レジスタの説明

| 名称 | ビットフィールド | 読み書き | 動作 | リセット値 |
|---|---|---|---|---|
| HPMx | 31:4 | RW | mhpmcounterxレジスタのカウント禁止ビット:<br>0: 正常にカウント<br>1: カウント禁止 | 0 |
| IR | 3 | RW | minstretレジスタのカウント禁止ビット:<br>0: 正常にカウント<br>1: カウント禁止 | 0 |
